import React from 'react';
import {{name}} from './';

export default {
  title: '{{name}}',
  component: {{name}},
};

const Template = (args) => {
  return <{{name}} {...args} />;
};

export const Main = Template.bind({});

Main.args = {
 // add default prop values here
};
